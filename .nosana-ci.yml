nosana:
  description: "Template Repo Pipeline"
  
global:
  image: registry.hub.docker.com/library/node:18

  trigger:
    branch:
      - main

  environment:
    SECRET_VALUE:
      type: "nosana/secret"
      endpoint: "https://secrets.k8s.dev.nos.ci"
      value: "0feffea5-5144-4671-9816-f37c04978fbd_npm-deploy-key"
      
jobs:
  - name: "Install Dependencies"
    commands:
     - echo trigger ci
      - npm ci
      - ls -lah